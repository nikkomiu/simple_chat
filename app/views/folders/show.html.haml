.panel.panel-default
  .panel-heading
    %h2.panel-title= @folder.name
  .list-group
    - if @folders.empty? && @rooms.empty?
      .panel-body
        .alert.alert-info
          %b Hey!
          Why not add something to this folder!
    - else
      = render @folders
      = render @rooms
