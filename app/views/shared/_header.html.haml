%h3.c-page-header
  = name
  .btn-group.pull-right
    %a.btn.btn-sm.btn-primary.dropdown-toggle{ type: 'button', 'data-toggle': 'dropdown' }
      %span.glyphicon.glyphicon-plus
      %span.caret
    %ul.dropdown-menu
      %li= link_to 'Folder', new_team_folder_path(params[:team_path] || params[:path])
      %li= link_to 'Chat Room', new_room_path
  .clearfix
